#include <stdio.h>

/**
 * main - Print a string : afficher une string 
 * donc chaine de caractere donc plus de un cara
 * Return: Always 0 : toujours 0
 */
int main(void)
{
    puts("\"Programming is like building a multilingual puzzle");
    return (0);
}

