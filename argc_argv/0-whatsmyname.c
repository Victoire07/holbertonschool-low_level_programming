#include <stdio.h>
/**
 * main -  Programme qui imprime son nom, suivi d'une nouvelle ligne
 * @argc: argument count (nombre)
 * @argv: argument vector (unused) (vecteur)
 *
 * Return: nbr d'arguments
 */

int main(int argc, char *argv[])
{
	(void)argc;
	 printf("%s\\n", argv[0]);
	  return (0);

}
