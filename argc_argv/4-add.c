#include <stdio.h>
/**
 * main - Programme qui additionne des nombres positifs
 * @argc: argument count
 * @argv: argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int somme = 0;
	int i;

	if (argc == 1)
	printf("0\n");
	return(0);

	for (i= 1; i <= argc; i++)
}
